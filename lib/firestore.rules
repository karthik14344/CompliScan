rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /products/{productId} {
      // Allow read access for dashboard viewing
      allow read: if true;
      
      // Allow write access (your backend will write here)
      allow write: if true;
    }
  }
}